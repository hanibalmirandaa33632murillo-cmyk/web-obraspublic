# frontend/.env
# En desarrollo local (con docker compose up):
VITE_API_URL=http://localhost:8000

# En produccion, cambia por la URL de tu backend en Railway:
# VITE_API_URL=https://obras-backend-production.up.railway.app
